{
  "name": "@testurio/adapter-pg",
  "version": "0.1.0",
  "description": "PostgreSQL adapter for Testurio DataSource",
  "keywords": [
    "testurio",
    "postgresql",
    "pg",
    "adapter",
    "testing",
    "e2e"
  ],
  "author": "Damir Yusipov",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/udamir/testurio.git",
    "directory": "packages/adapter-pg"
  },
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --sourcemap --clean",
    "test": "vitest run",
    "lint": "biome check src",
    "prepublish": "npm pkg set version=$(node -p \"require('../../package.json').version\") && pnpm run lint && pnpm run test && pnpm run build"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "peerDependencies": {
    "testurio": "^0.4.0"
  },
  "devDependencies": {
    "testurio": "workspace:*",
    "@biomejs/biome": "^2.3.11",
    "@types/node": "^24.10.0",
    "@types/pg": "^8.11.10",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^1.6.0"
  }
}
