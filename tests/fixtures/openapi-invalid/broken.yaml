openapi: 3.0.0
info:
  title: Broken YAML Spec
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        '200': { description: "unterminated string
          content:
            application/json:
              schema:
                type: object
