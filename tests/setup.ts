/**
 * Test Setup
 *
 * Setup file for tests.
 * This file is imported by vitest.config.ts as a setup file.
 * 
 * Note: Adapters are now passed directly to components, so no global
 * adapter registration is needed. Tests import adapters directly.
 */

// No global setup needed - adapters are passed directly to components
